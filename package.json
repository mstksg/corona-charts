{
  "scripts": {
    "postinstall": "spago install",
    "build": "spago build",
    "clean": "rm -rf node_modules output .spago dist/* *.lock",
    "bundle": "spago bundle-app -x --main Main --to dist/app.js && parcel build assets/index.pug -d _site --public-url ./ --no-content-hash --no-minify",
    "deploy": "spago bundle-app --main Main --to dist/app.js && parcel build assets/index.pug -d _site --public-url ./ --no-source-maps --no-content-hash && jle-update-gh-pages _site && git push origin master gh-pages"
  },
  "devDependencies": {
    "cssnano": "^4.1.10",
    "parcel-bundler": "^1.12.3",
    "parcel-plugin-cname": "^1.0.0",
    "parcel-plugin-static-files-copy": "^2.3.1",
    "pug": "^2.0.4",
    "purescript": "^0.13.3",
    "sass": "^1.26.5",
    "spago": "^0.9.0"
  },
  "dependencies": {
    "jstransformer-markdown-it": "^2.1.0",
    "marked": "^0.7.0"
  },
  "staticFiles": {
    "staticPath": "public",
    "watcherGlob": "**"
  },
  "homepage": "https://coronavirus.jle.im"
}
