{
  "scripts": {
    "postinstall": "spago install",
    "build": "spago build",
    "clean": "rm -rf node_modules output .spago dist/* *.lock",
    "bundle": "spago bundle-app --main Main --to dist/app.js && parcel build assets/index.html -d dist/corona-charts --public-url /corona-charts/ && jle-update-gh-pages dist/corona-charts"
  },
  "devDependencies": {
    "cssnano": "^4.1.10",
    "parcel-bundler": "^1.12.3",
    "purescript": "^0.13.3",
    "spago": "^0.9.0"
  },
  "dependencies": {
    "marked": "^0.7.0"
  }
}
